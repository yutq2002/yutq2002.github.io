---
import type I18nKey from "@i18n/i18nKey";
import { i18n } from "@i18n/translation";

interface Props {
	k: I18nKey;
	class?: string;
}

const { k, class: className } = Astro.props;
---

<span data-i18n-key={k} class:list={className}>{i18n(k)}</span>
